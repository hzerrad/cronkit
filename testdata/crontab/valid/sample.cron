# Sample crontab file for testing
# This file contains various types of cron entries

# Environment variables
SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin
MAILTO=admin@example.com

# Daily backup at 2am
0 2 * * * /usr/local/bin/backup.sh

# Check disk space every 15 minutes
*/15 * * * * /usr/local/bin/check-disk.sh # Disk monitoring

# Weekly report on Mondays at 9am
0 9 * * 1 /usr/local/bin/weekly-report.sh

# Monthly cleanup on the 1st at midnight
@monthly /usr/local/bin/cleanup.sh

# Hourly health check
@hourly /usr/local/bin/health-check.sh

# Complex command with pipes
0 3 * * * cd /var/log && find . -name "*.log" -mtime +30 -delete

# Job with arguments
30 14 * * * /usr/local/bin/process.sh --config=/etc/app.conf --verbose

# Multiple jobs on weekdays
0 9-17 * * 1-5 /usr/local/bin/business-hours.sh

